// frontend/src/contractConfig.js
// Fill in CONTRACT_ADDRESS after running scripts/deploy.js
// (or after deploying via Remix and copying the deployed address).

export const CONTRACT_ADDRESS = "0xYourDeployedContractAddressHere";

// Paste the ABI from artifacts/contracts/LandRegistry.sol/LandRegistry.json
// (the "abi" field) after running `npx hardhat compile`.
export const CONTRACT_ABI = [
  // ... generated ABI goes here ...
];
